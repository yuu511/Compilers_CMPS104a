.struct madokami
.field ptr place
.field int wish
.end 
.struct homura
.field struct madokami madomado
.field int madoka
.end 
.struct mami
.field ptr madoka
.field int one
.field struct mami mamimami
.field struct homura homu
.field ptr two
.end 
.s0:       "hehe"
.s1:       "hehexd"
a:         .global int 
b:         .global int 5
c:         .global int 'a'
w:         .global ptr 
x:         .global ptr (.s0)
y:         .global ptr nullptr
thing:     .global struct mami nullptr
thing2:    .global struct mami call malloc(sizeof struct mami)
thing3:    .global ptr nullptr
sayaka:    .function int 
           .param int x
           .param int y
           return
           .end
filler:    .function int 
           return
           .end
trivial:   .function 
           .param int x
           .local struct homura madoka
           .local struct mami xa
           .local struct madokami kyubey
           madoka = call malloc(sizeof struct homura)  
           xa = call malloc(sizeof struct mami)  
           kyubey = call malloc(sizeof struct madokami)  
           $t0:p = madoka->homura.madomado  
           $t0:p->madokami.place = (.s1)  
           kyubey->madokami.place = $t0:p->madokami.place  
           return
           .end
