.struct homura
.field int madoka
.end 
.struct mami
.field ptr madoka
.field struct homura homu
.field struct mami mamimami
.field int one
.field ptr two
.end 
.s0:       "hehe"
x:         .global ptr (.s0)
sayaka:    .function int 
           .param int x
           .param int y
           return
           .end
filler:    .function int 
           return
           .end
trivial:   .function 
           .param int x
           .local struct homura madoka
           .local struct mami thing2
           madoka = call malloc(sizeof struct homura)  
           thing2 = call malloc(sizeof struct mami)  
           $t0:p = thing2->mami.homu  
           $t0:p->homura.madoka = 5  
           return
           .end
