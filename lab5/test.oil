.struct mami
.field ptr madoka
.field struct sayaka sayaka
.field int one
.field ptr two
.end 
.struct sayaka
.end 
trivial:   .function 
           .param int x
           .local ptr madoka
           .local ptr mami
           .local ptr homura
           .local ptr sayaka
           $t0:p = call malloc(5)
           homura = $t0:i  
           mami = $t0:i  
           madoka = $t0:i  
           sayaka = madoka  
           return
           .end
