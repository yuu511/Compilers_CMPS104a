#include "oclib.h"
int fac (int n){
  int f = 1;
  while (n > 1) {
    f=f*n;
    n=n-1;
  }
  return f;
}

int main() {
  int n = 1;
  while(n <= 5) {
    putint(fac (n));
    n = n + 1;
  }
  return SUCCESS;
}
